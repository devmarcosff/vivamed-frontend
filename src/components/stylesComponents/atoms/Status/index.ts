import StatusIcon from './status.icon';
import StatusRoot from './status.root';
import StatusText from './status.text';

export { type StatusType } from './status.types';

export const Status = {
  Root: StatusRoot,
  Text: StatusText,
  Icon: StatusIcon,
};
